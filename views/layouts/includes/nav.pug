nav(class='navbar navbar-expand-lg navbar-dark bg-dark')
  a(class='navbar-brand mb-0 h1', href='#') Películas
  button(class='navbar-toggler', type='button', data-toggle='collapse', data-target='#navbarNav', aria-controls='navbarNav', aria-expanded='false', aria-label='Toggle navigation')
    span(class='navbar-toggler-icon')
  div(class='collapse navbar-collapse' id='navbarNav')
    ul(class='navbar-nav mr-auto')
      li(class='nav-item')
        // a(class='nav-link', href='/home') Stats
      li(class='nav-item')
        a(class='nav-link', href='/movies') Movies
      li(class='nav-item')
        // a(class='nav-link', href='/series') Series
    div.float-right 
      ul(class='navbar-nav')
        li(class='nav-item')
          //input(class='form-control mr-sm-2', type='search', placeholder='Search Movie', arial-label='Search')
        if login
          // li(class='nav-item')
            a(class='nav-link', href='/dashboard') Dashboard
          li(class='nav-item')
            a(class='nav-link', href='/form') Form
          li(class='nav-item')
            a(class='nav-link', href='/users/logout') Logout
        else
          li(class='nav-item')
            a(class='nav-link', href='/users/signin') Login